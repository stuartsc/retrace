# Progressive recall validation

Started 2026-09-11 on `feature/push-to-dictate`. Baseline source: `5b364cbf4ac6419b0ad0745729a57986695dec78`.

This is an implementation/acceptance record. Latest checkpoint on **2026-09-14**: **866 tests, five skips, zero failures**; signed **2609.14.1** installed and launched after the notification correction. The live database remains V21, and the previous app is retained for rollback with the current library. The earlier whole-library recovery copy and copied-database migration/reader checks remain retained. Phase 0/1 source and validation are committed for continued development. Installed recall acceptance remains in progress; this is an unreleased local trial. See [the latest correction and installed checks](#notification-correction--2026-09-14). The saved plan is [progressive-recall-plan.md](progressive-recall-plan.md).

## Phase 0 baseline

Observed environment: Apple Silicon, macOS 26.1 (25B78), Swift 6.2.4. Installed Retrace is 0.7.6 (2609.9.1); installed Timely Memory is 26.8.176 (`com.timeapp.devlpmp.Timely-Mac-Tracker`). Installation does not establish that Timely is collecting or that settings match.

Read-only Timely UI inspection confirmed tracking **On**, signed in, last upload shown as **12 minutes ago**, with **Idle** status at inspection. Idle detection is enabled and discards held activity after 15 minutes; fullscreen applications prevent idle detection for one hour. No per-app idle exemptions were selected. Incognito exclusion is enabled and no additional ignore condition was visible. These settings were preserved. This observation is a scope/health baseline, not a matched comparison result.

The unmodified-source baseline `swift test` executed **596 tests, 4 opt-in skips, 2 unexpected failures** in 242.457 seconds. Both failures are an absent `~/ScreenMemoryData/screenshots` folder in `AsyncQueuePipelineTests.testProcessScreenshotsThroughAsyncQueue` and `OCRPipelineTests.testProcessScreenshotsThroughFullPipeline`. The baseline log is local `/tmp/retrace-progressive-recall-baseline.log`; raw test logs are not committed because existing tests can log captured content.

The first executable recall-search regression run executed **11 tests with 57 assertion failures, no unexpected failures**, in 5.327 seconds. It reproduces constrained results hidden below 2,000 global matches, title/position evidence loss, truncated keyword results when dates are the only filter, invalid page limits, rank cursor loss and unsupported geometry. These use real temporary SQLite/FTS databases; they establish query correctness failures, not real-work recall quality. The full-text API requires a keyword; these tests do not establish empty-query browsing.

## Version 1 questions and independently reviewable oracles

The fixed workload specifies two documents with the same displayed title, a chat pane and an interruption. The two screen fixtures supplied with this change contain intentionally authored, non-private content. An independent reviewer was given only their rendered images before the oracle and confirmed the amounts, statuses and negation; see [the versioned oracle](fixtures/progressive-recall/oracle.json). These reviewed images are reproducible OCR evidence, not a corpus of actual user questions or proof that the full Mac action script ran.

| ID | Question | Required evidence / failure state |
|---|---|---|
| Q01 | What amount was in the first draft of the Cedar proposal? | First rendered document: `Amount: 42000`, `Status: DRAFT`, timestamp order preserved. |
| Q02 | What changed in the later proposal with the same title? | Later rendered document: `Amount: 47000`, `Status: SENT`, `Approval: NOT GRANTED`; both observations retained. |
| Q03 | Which document was focused while OCR was backlogged? | A persisted document/window activity event with uncertain identity stated when no stable ID exists. No content claim from metadata alone. |
| Q04 | Reopen the screen that supports the later amount. | Exact store/source/observation/frame/revision, or explicit unavailable/integrity result. Zero neighbouring or native/Rewind substitutions. |
| Q05 | Was the two-second chat glance retained? | A notified event or disclosed unknown coverage. Presentation grouping never deletes the event. |
| Q06 | Did the user spend the whole compile waiting in the editor? | Focus and episode span are distinct; background execution cannot be counted as user attention. |
| Q07 | What happened while capture was paused/asleep? | Administrative boundary and honest gap, no inferred continuous focus or captured app/title during the pause. |
| Q08 | Does an excluded window or credential-bearing URL appear anywhere? | No excluded metadata or raw URL secrets in new storage/cache/log/feed/export sinks. |
| Q09 | Can an older citation survive OCR repair when its media is missing? | Retained text/provenance plus media-unavailable receipt; no automatic evidence deletion. |
| Q10 | Does undo of a confirmed project correction change recorded facts? | Append-only revoke and visible pending/conflict/application status; original activity unchanged. |

## Matched Mac / Timely action script

Run with both versions and collection scopes recorded, preserving existing macOS power settings. Use a dedicated test document/conversation and review the resulting recording. Timely is a comparison product, not ground truth.

1. Note UTC wall time, uptime, display resolution/scale, capture/OCR settings, power/thermal state and initial queue age. Record Timely enabled/paused/ignore settings and version.
2. Open the first Cedar proposal for 20 seconds, then a separate same-title document with the changed amount/status for 20 seconds. Rename only the latter, and return to the first.
3. Switch to a chat/conversation for two seconds, then return. Repeat with a same-title tab whose URL changes. Include a chat canvas/pane if available.
4. Read unchanged content for 60 seconds. Run a compile/background agent while switching to another document. Do not infer execution from the agent app being visible.
5. Repeat the document switches during an OCR backlog; verify metadata search before OCR completion and without installing a model.
6. Pause/resume, sleep/wake, and inspect activity gaps. Test an excluded test window and credential-shaped test URLs. Do not enter actual credentials.
7. Ask Q01–Q10 against each retained corpus. Record correct attribution, missing/late transitions, focus-duration error, gaps, query-to-exact-screen time and unresolved cases.
8. Repeat the same script/settings on the candidate. Measure notification-to-durable p50/p95, capture-to-search p50/p95, fresh queue growth/capture gaps, foreground/dictation latency, memory/CPU/disk growth. A percentile improvement cannot conceal missing events or a growing queue. Foreground p95 must not worsen by more than 10%.

**Matched Timely results: pending. Installed-app discovery is not comparison evidence.** Do not mark a local trial or Phase 1 acceptance complete until the exact-navigation and matched-workload gates are run.

## Implementation acceptance ledger

| Area | Automated evidence | Live acceptance |
|---|---|---|
| Constraint placement, all distinct observations, pagination | 27 focused SQLite search tests pass, including a 10,003-row keyset fixture and native/imported revision invalidation. Filters apply before the page limit; duplicate document links cannot duplicate a frame or manufacture a conflicting selected text. | Actual user questions pending; authored Word recordings checked separately below |
| Durable activity, feed/checkpoints, annotations | 91 focused database tests pass for transactions, restart, rollback, cursor/checkpoint monotonicity, correction acknowledgement/revoke and validated screen links. The projection preserves gaps, two-second interruptions and selected correction boundaries. | Native Word and generic Claude window observations pass; brief-switch, pause/sleep, overload and remaining adapters pending |
| Historical OCR and media repair | 26 focused Processing tests pass with real Vision, SQLite and media. The two complete rendered JPEG → HEVC → Vision → SQLite → FTS pipeline tests pass without an external screenshot folder. Historical OCR uses pixels and saved metadata; missing media leaves retained text and provenance available. | Live old-frame/current-Accessibility adversarial check pending |
| Exact source/frame/revision and journal resolution | 21 strict encoded-media/journal tests pass. The final 53 search/resolver/revision tests include disconnected/replaced sources, same-path replacement behind a still-open SQLite handle, cancellation, privacy, unavailable media and retained revisions. Unchanged legacy evidence can be previewed/materialized without invalidating its original hit, sibling results or pagination; actual index/source/deletion changes still invalidate stale selections. Source identity is revalidated across asynchronous work. | Two finalized Word frames pass copied-library search, service resolution and immutable reopen with the private-alias adaptation below. Installed hidden/visible/previously-live UI navigation and the live journal path remain pending |
| Metadata timeline and stage health | 27 focused presentation tests pass, including real SQLite metadata search before any OCR, linked evidence paging, explicit correction confirmation, delayed query/projection/correction/privacy-setting guards, association deletion during retained-text retrieval, successful writes with failed status readback and short visits inside grouped episodes. Six projection tests cover gaps, corrections and inherited-rule precedence. | Native Word metadata search before images/OCR passes; useful recall during a real backlog and installed UI smoke checks pending |
| Search thumbnails | 13 real SQLite/service tests pass for source replacement, denied/deleted evidence, cancellation, request identity, queue cap and bounded decoding. Old disk/memory preview caches and mutable OCR crops are bypassed. | Preview latency and scrolling under the matched workload remain unaccepted |
| Capture attribution and consequential pixel changes | The initial 54 focused Capture tests pass. Separate one-digit amount fixtures at 1×/2× exposed a real loss in the sampled detector. The later SIMD detector has 26 passing behavioral tests; its strict CPU gate remains open as measured below. | Word document identity and generic Claude context pass; remaining app/pane fixtures, prompt durability and complete capture cost remain unaccepted |
| Recording startup/stop | Five lifecycle tests pass, including a real AsyncStream and delayed file finalization before replacement capture begins. Concurrent explicit pause also stops activity while joining an unexpected screenshot stop. | Live recording, pause/resume and shutdown smoke checks pending; the tests do not establish that every inherited audio child task drains |

## Implementation and review details

- Activity collection is a separate, initially disabled control, gated by the existing master recording permission/pause state. App activation is stored before bounded window/document enrichment; later enrichment starts at its actual observation time. Screenshot failure does not silently turn off the independent activity stream. Observer loss, exclusion, overflow and persistence recovery have explicit coverage/health states.
- The additive V21 migration provides durable activity/feed/checkpoints, confirmed correction receipts, a native/imported store registry, immutable extraction snapshots, validated activity-to-screen links and search revision fences. Compatibility OCR writers also append snapshots transactionally; flat legacy text does not receive unsupported geometric highlights. Imported databases remain read-only.
- The timeline offers episodes, retained intervals and exact evidence. Grouping/hiding changes presentation only. Selected corrections do not spill into adjacent or future observations; unacknowledged corrections stay visibly pending. Metadata search does not require OCR or a model download. Direct screen search is independent of episode labels.
- Exact references preserve source, store, observation, frame and extraction revision. Encoded samples and journal records are verified before disclosure. Current source/privacy/deletion state is checked again after asynchronous reads; a stale result cannot silently use another store or newer text. Missing images can expose the retained cited text with a separate unavailable-image state. Agent disclosure currently fails closed; no new external agent access was enabled.
- URL metadata drops query/fragment/userinfo and unsafe opaque path components before new capture sinks. The raw navigation value is reduced to a local identity hash so scrubbed URLs do not merge separate contexts. This is a tested conservative policy, not a claim to recognize every possible secret.
- Independent review found and prompted regressions/fixes for stale source selections, delayed timeline projection, removed activity associations, correction scope, startup/pause races and capture writer finalization. Source-qualified thumbnails are included in the same disclosure boundary as exact selection.

## Performance and coverage limits

The initial debug-instrumented 10,003-row synthetic search receipt recorded 78 full pages: p50 **89 ms**, p95 **109 ms**, maximum **153 ms**. A complete traversal requires **79 pages**, including the final partial page. Optimized Swift and SQLCipher measurements are recorded in the continuation evidence below; debug timing is not an optimized-runtime claim.

The initial optimized scalar pixel safeguard measured **2.43–2.68 ms CPU** for independently rendered duplicates and **31.35–32.35 ms** for forced full scans (five repetitions in each of three runs). The subsequent SIMD implementation and its measured limits are recorded below. Reviewed one-digit fixtures survive both scale factors and the reviewer checked all 1,024 tile alignments; arbitrary small semantic changes remain outside that evidence.

Focused coverage receipts show exact encoded reads at **96.13% lines**, exact journal reads at **98.21%**, the detector at **100%**, the thumbnail helper at **99.12%**, and the timeline view model at **96.43%** before the final race follow-ups. Native observer bridges and the wider processing queue remain below 80% in these focused runs. No whole-module coverage, live foreground-p95, energy, daily disk-growth or native-adapter acceptance claim follows from these numbers. Thumbnails now decode on each appearance, capped at two active requests and 32 queued requests; that disclosure safeguard needs live scrolling/performance measurement.

## Initial complete-package verification — 2026-09-12

`swift test --enable-code-coverage` built the complete package and passed **814 tests, four intentional skips, zero failures** in **324.564 seconds**. The skips are the interactive Accessibility inspector, live FuseIntel wire-contract check and two explicitly provisioned native-speech/audio comparisons. Their real-environment gates remain pending. The final log is `/tmp/retrace-progressive-recall-green.log`; code coverage is generated under `.build/arm64-apple-macosx/debug/codecov/`.

Selected production-file coverage from that complete run:

| File | Lines | Regions |
|---|---:|---:|
| `ActivityPersistence.swift` | 97.67% | 90.23% |
| `ScreenEvidencePersistence.swift` | 98.27% | 93.75% |
| `V21_ProgressiveRecall.swift` | 100% | 100% |
| `ActivityTimelineProjection.swift` | 93.43% | 91.53% |
| `RecordingLifecycle.swift` | 97.40% | 97.14% |
| `ProgressiveRecallService.swift` | 88.70% | 72.77% |
| `ActivityTimelineViewModel.swift` | 96.76% | 86.43% |
| `SearchEvidenceThumbnailLoader.swift` | 99.12% | 92.31% |
| `ExactFrameReader.swift` | 94.48% | 87.30% |
| `FrameDeduplicator.swift` | 100% | 89.53% |
| `ActivityMonitor.swift` | 51.74% | 51.77% |
| `CaptureAttribution.swift` | 52.53% | 47.46% |

The native observer/adapter coverage remains below the 80% target even in the complete run. Installed application fixtures and live lifecycle acceptance are required; the passing suite is not a blanket coverage or production-readiness claim.

The integrated run found two older test expectations needing alignment with reviewed behavior: canonical URL encoding must preserve the Unicode path, and changed imported text must append a revision while retaining the original citation. The strengthened 44-test persistence subset passed before the final complete run. No production behavior was weakened to satisfy those expectations.

Independent code review closed all reported findings and approved the final source, subject to the live acceptance gates above. `git diff --check` and `scripts/check_no_nanoseconds_sleep.sh` pass. At this checkpoint, changes remained local and uncommitted on `feature/push-to-dictate`, based on `5b364cbf4ac6419b0ad0745729a57986695dec78`; no installation, live-library migration or release occurred. Local `/tmp` logs are disposable; the test sources, fixtures and this ledger are included in the repository change and are available to Git.

## Isolated native UI check — 2026-09-12

The current timeline/view model was compiled into a temporary macOS fixture application using a real in-memory SQLite database and authored activity events. It did not initialize capture or open the user's recorded library. Computer-use inspection verified episode expansion, the preserved two-second interruption, activity metadata selection, explicit absence of a linked image, and short-visit hide/reveal with the surrounding document intervals retained. A selected rename displayed its pending receipt and split only the affected interval; other document titles remained unchanged.

The visual check found that short visits inside grouped episodes were not counted by an episode-only filter; the corrected interval filter has a real SQLite regression and was visually rechecked. It also found an empty rename label disabled Cancel; validation now disables Save/Confirm only. The fixture window is validation of these current native views, not an installed recording trial, full historical-navigation acceptance, measured responsiveness result or Timely comparison.

## Delivery boundaries

Phase 1 is implemented in additive local slices with separate acceptance status. The local event/correction outbox is the contract boundary for FuseIntel; another repository's consumer and acknowledgement service are not implied by a Retrace commit. Confirmed commands remain pending until acknowledged. Original activity and exact evidence remain readable offline.

No live library cleanup, model download, application replacement or release is established by the automated tests. Keep the installed application and preferences for a matched comparison; an eventual candidate install must retain a compatible data rollback path for the additive migration.

## Continued acceptance — 2026-09-12 afternoon

Read-only refresh confirmed `/Applications/Retrace.app` is still **0.7.6 (2609.9.1)**, signed by SIMPLE CLICK PTY LTD (`CC88WZ5SQ7`), with the existing recorder running. Timely Memory **26.8.176** was **not running** at this refresh. Its earlier tracking-on observation is historical and cannot establish today's matched collection. No Timely setting, live library migration or installed application replacement was performed during these checks.

### Native Word adapter fixtures

`Capture/Tests/InstalledActivityAdapterTests.swift` is an opt-in acceptance test. It samples the actual frontmost process, WindowServer window/display and focused AX document, then starts the production native activity observer against a unique temporary disk SQLite database. It searches the reviewed title before any images or OCR exist and checks the expected document identity. Other running apps are excluded for this controlled fixture, a changed foreground app fails the test, and temporary data is closed and removed. The test does not initialize capture or change live settings. It fails closed when live encryption is enabled so the temporary disk database cannot create or replace the production Keychain key.

On installed **Microsoft Word 16.113 (16.113.26090931)**, independent rendered-screen inspection confirmed an unsaved `Document1` containing **42000 / DRAFT / NOT GRANTED**, followed by two separately saved `Cedar proposal` documents containing **42000 / DRAFT** and **47000 / SENT**, both with **NOT GRANTED**. These are authored local fixtures, not client documents. The source text is reproducible from the two [Word RTF fixtures](fixtures/progressive-recall/word/first/Cedar%20proposal.rtf); the corresponding second file is in `word/second/`. Word-created DOCX files remain local under `/tmp/retrace-installed-adapter-fixtures/word/`.

| Reviewed surface | Native adapter result | Search-before-OCR result |
|---|---|---|
| Unsaved Document1 | Window and real display observed; document/pane identity explicitly unavailable | Pass: one matching durable event, zero images |
| First saved Cedar proposal | Exact first local file URL and its opaque document identity | Pass: two matching durable events, zero images |
| Second saved Cedar proposal with the same title | Exact second local file URL and its distinct opaque document identity | Pass: two matching durable events, zero images |

Each opt-in run passed with zero failures. Native sampling took **88 / 154 / 248 ms** in these three debug-instrumented runs. Observation-to-persistence-timestamp wall-clock deltas were **10.5 ms**, **14.8–15.5 ms**, and **40.5–224.4 ms** respectively. The writer assigns that timestamp before INSERT/metrics/COMMIT, so these values do not measure durable acknowledgement. These few startup samples are not notification-latency percentiles, focus-transition coverage, or an optimized full-app performance benchmark. The first setup attempts correctly failed before database startup because an automation-visible Word window was not the actual foreground app; Finder's normal application Open action established real focus for the accepted runs.

Local evidence: `/tmp/retrace-word-unsaved-accepted.log`, `/tmp/retrace-word-first-accepted.log`, `/tmp/retrace-word-second.log`. The test and fixture definitions are versioned; raw logs remain local. Pane identity, real rename continuity, rapid same-app notifications and other priority applications remain separate gates.

To rerun, first independently review and foreground the intended authored window. Set `RETRACE_INSTALLED_ADAPTER_FIXTURE` to a JSON file containing `appBundleID`, exact `windowTitle`, expected `adapter`, `documentUnavailable`, and optionally `documentURL`. Exactly one of a URL or explicit unavailable state is required. Run `swift test --filter InstalledActivityAdapterTests`; unprovisioned automated runs skip this interactive gate. The test prints a content-free `INSTALLED_ADAPTER_RECEIPT` only after its observations and search assertions succeed.

### Claude and restricted application coverage

Installed Claude **1.52386.3** passed the same native observer → temporary SQLite → metadata-search check with one durable event and zero images. Native sampling took **84.4 ms**; the single observation-to-persistence-timestamp wall-clock delta was **9.2 ms**, with the same pre-COMMIT limitation as the Word samples. Its native window title was the generic `Claude`, with document and pane identity explicitly unavailable. Independent screen inspection showed a conversation title within the window, so this result does **not** establish conversation-title extraction. No conversation content was submitted or changed. The local receipt is `/tmp/retrace-claude-window.log`.

Computer use refused access to `com.openai.codex` for safety reasons. No alternative API or identifier was used to inspect that blocked application. Its installed visual/adapter acceptance remains open.

### Recorded Word media check

The still-installed recorder retained the authored Word examples. Read-only SQLite queries mapped frame **50758070** to encoded sample **52**, and frame **50758079** to sample **61**, in video **1010331**. The current production `ExactFrameReader` decoded both requested samples at **30 fps / 3024×1964**. Independent inspection confirmed **42000 / DRAFT / NOT GRANTED** in the first and **47000 / SENT / NOT GRANTED** in the second.

The first legacy row's stored title was `Cedar proposal`, while its decoded pixels still showed `Document1`. This is an observed legacy metadata/pixel mismatch; the candidate must preserve its uncertainty. The decoder probe used the separately recorded database mapping and does not establish the complete search-selection resolver, installed historical UI routing or new capture attribution. Raw recorded images remain local. The mapping receipt is retained with candidate preparation artifacts; decoder output is under `/tmp/retrace-recorded-word-navigation/`.

A later copied-library search/service probe found two eligible rows and created the selected reference for frame **50758070**, then returned **`frameFinalising`** before exporting pixels. Both copied frame rows report encoding success, but their video has **processingState=1**, meaning unfinished. The probe's encoded-media loader deliberately returns this state for unfinished segments; the production App coordinator additionally attempts the strict journal reader. This attempt therefore does not establish failure or success of the app's journal-backed path. No finalization flag was changed to force an image result. One immutable legacy observation/extraction was materialized in the trial copy.

The first attempt also could not save its failure receipt inside the process sandbox. A reviewed diagnostic-only adjustment sent content-free stage/error fields through stdout and exclusively saved them in the private output directory, with no sandbox expansion. The resulting unavailable receipt and stored metadata are retained as `cedar-service-unavailable.json` and `cedar-copied-media-state.json` in the candidate directory. This is an incomplete image-reopen check, distinct from the successful direct sample decodes above.

### Finalized recorded-frame search and immutable reopen

A separate bounded query reviewed at most **20 Word frame metadata rows** from finalized videos. It selected frames **50736632** and **50736634**, encoded samples **61** and **63** in finalized video **1009966**, from 2026-09-08. The source manifest fixes the original file path, device/inode, capture timestamps, frame count, dimensions and frame/sample mapping. No source video or legacy finalization metadata was changed.

The first attempt used an empty keyword and found no results. Source and regression review confirmed that `DataAdapter.search(query:)` is a full-text API requiring a keyword, matching the existing UI boundary. Date constraints in the pagination regressions accompany a keyword; they do not establish empty-query browsing. The accepted run used a common indexed term with the same bounded dates and Word filter. Recorded document content is kept out of the numeric receipt.

The next attempt reached the reader and returned an integrity failure because the process sandbox prevented creation of its temporary `.mp4` alias for an extensionless media file. Foundation's temporary directory remained outside the private probe directory despite an explicitly set `TMPDIR`. A reviewed harness adaptation exclusively creates and validates the alias inside its existing allowed directory. Through an existing test seam, `ExactFrameReader` still validates the **original pinned media** and invokes its **unmodified production decoder** through that private alias. No source check, timestamp/dimension check or sandbox restriction was relaxed; production code was unchanged. Default public-reader alias placement, the App coordinator's loader and the journal path are outside this adapted probe's evidence.

The final run passed. Both source-qualified search hits resolved at **3024×1964**; selecting A, then B, then reopening their immutable links preserved each frame's pixel, text, title and reference hashes. A and B have distinct pixel/text/reference hashes and the same stored-title hash. Both retain **legacy context=true** and **verified highlights=false**. Agent-audience requests remained denied, and a missing revision remained unavailable for each frame. This is service/media acceptance on real retained recordings, not installed UI navigation or an actual-user question corpus.

The numeric receipt is retained as `finalized-word-service-acceptance.json` in the candidate directory. Raw images and unsuccessful attempts remain private under `/tmp/retrace-finalized-word-service-acceptance/`; they are not added to Git.

Independent receipt review confirmed all four **3024×1964** decoder returns, byte-identical original/reopened PNG pairs and different A/B images. The output directory is **0700**, all four PNGs and the receipt are **0600**, and the private alias directory is empty after completion. Trial-copy materialization is limited to exactly **three observations and three extractions**: the earlier Cedar selection and these two finalized frames. The native search revision remains zero, so unchanged materialization did not invalidate the selected evidence. The post-run read-only verification made zero changes; its receipt is `finalized-word-service-post-run.json`.

Independent visual inspection of the Word region confirmed coherent recorded screens: the first contains an application context menu, and the second shows Word's save-changes dialog over the dimmed document. This is a visible interface-state distinction, not proof that the document's contents changed. No background Codex interface was inspected and no private document text is reproduced in this record. Temporary review crops were removed.

### Pixel comparison optimization

Native 16-byte SIMD now computes the same inclusive 48-level RGB difference into a changed-pixel mask per tile row. Adjacent-pair counting preserves the six-pair/three-row admission rule. Alpha-only differences, low-contrast noise, partial and unaligned rows, malformed buffers, lower sensitivity levels and independent duplicate buffers retain regression coverage. Scratch counters use **360 bytes at 4K**, plus fixed vectors and input image buffers.

Three optimized runs used 20 warmed samples and three cache-disturbed samples per case. The latter are a limited cold receipt whose reported p95 is its maximum, not a reliable population percentile. At **3840×2160**:

| Case | Warm CPU p95 | Cache-disturbed CPU maximum | Warm wall p95 |
|---|---:|---:|---:|
| Independently rendered duplicate | 1.759–2.361 ms | 4.499–7.410 ms | 2.387–4.016 ms |
| Low-contrast full scan | 2.930–5.084 ms | 4.330–6.358 ms | 3.711–9.098 ms |
| Isolated high-contrast full scan | 2.927–4.789 ms | 4.148–5.789 ms | 4.056–8.148 ms |

All behavioral assertions passed. The unchanged strict **5 ms CPU** gate passed in one run and failed in two; stable detector and foreground-performance acceptance remain open. The separate instrumented run passed **26 tests, zero failures**, with **100% line/function and 94.32% region coverage** for the detector. Independent review approved the source and regression boundaries. Local receipts and exact compiler/source hashes are in `/tmp/retrace-dedup-perf/RECEIPT.md` and `final-measurements.json`.

### Optimized search measurements and retained query shape

The opt-in search harness now fails before writing a receipt if traversal repeats a cursor, exhausts its bound, loses/duplicates evidence, or returns an empty timing population. Its output-path guard uses SQLite's actual opened filename and rejects the source database, sidecars and canonical aliases as output destinations. The real SQLite/filesystem guard test reproduced **10 expected failures** before the fix. Both the production query and a test-only experiment subsequently passed **27 focused tests** each, including native/imported sources, filters, duplicate document links, ranked ties, counts and selection/cursor revision fences. Independent review is closed.

Both shapes returned all **10,003 observations in 79 pages**, repeated three times. In the last coordinated quiet window, production page p95 was **28.65–28.81 ms**, versus **37.12–49.44 ms** when page IDs were materialized before loading their metadata. Earlier unchanged-production runs had p95 **43–77 ms** and maxima up to **183 ms**; the quieter repeat is not a source-code speedup. These are optimized Swift/SQLCipher synthetic adapter measurements, not full UI latency.

The opt-in read-only comparison also exercised the installed legacy library's core SQL, without adding V21 tables or running the candidate app. All **36 statements** completed with unchanged source identity/version and no duplicate results. All **12 comparisons** returned identical ordered frame IDs and rank bits; reports contain numeric labels and aggregate measurements, not recorded content or result IDs. For broad queries, steady ranking elapsed time fell from roughly **294–319 ms** to **208–212 ms**, and VM work from **6,405,990** to **4,605,921** steps. Exact counting still cost **87–100 ms** alone. Small/exact queries did not improve consistently. Baseline-before-experiment ordering and cache effects limit wall-time conclusions; these probes also omit V21 evidence joins and UI rendering.

The production query is retained because the experiment is not a general improvement. Existing indexes are used; neither an additional index nor a result-count cache was justified by this evidence. Broad-query latency and the end-to-end **100 ms** search target remain open. Local artifacts: `/tmp/recall-shape-production.json`, `/tmp/recall-shape-page-first.json`, `/tmp/recall-shape-live-comparison.json`; earlier measurements remain separately in `/tmp/recall-optimized-acceptance.json` and `/tmp/recall-live-core-acceptance.json`.

Reproduce the synthetic benchmark with `RETRACE_SEARCH_BENCHMARK=1 RETRACE_SEARCH_BENCHMARK_RUNS=3 RETRACE_SEARCH_BENCHMARK_OUTPUT=/tmp/recall.json swift test --scratch-path /tmp/retrace-recall-benchmark -c release --filter ProgressiveRecallSearchTests/testRelevantKeysetsReachTenThousandRowsWithoutRankTieSkipsOrGlobalCaps`. The additional `RETRACE_SEARCH_PAGE_FIRST_EXPERIMENT=1` flag substitutes only the test connection's query. `RETRACE_SEARCH_BENCHMARK_READONLY_DB` accepts an explicit database path for bounded, numeric-only legacy core comparisons. Production behavior does not depend on these test-only flags.

### Complete-package follow-up and migration compatibility finding

Before the compatibility correction below, `swift test --enable-code-coverage` passed **821 tests, five intentional skips, zero failures** in **311.931 seconds**. The additional skip is the explicitly provisioned installed-application fixture, which passed separately for the reviewed Word and Claude surfaces. The log is `/tmp/retrace-progressive-recall-continuation-tests.log`. Detector coverage is **100% lines/functions and 94.32% regions**; native observer/attribution coverage remains **51.74% / 52.53% lines**, so the interactive adapter results do not establish comprehensive native-bridge coverage.

Optimized compilation completed in **413.13 seconds**. Candidate **2609.12.1** passed strict signature, dependency, resource and entitlement review using the installed Developer ID (`CC88WZ5SQ7`). It was **superseded before installation** by the copied-library compatibility finding below and is retained with an `.app.disabled` suffix. It must not be installed. Its signed executable SHA-256 is `70480a28d569966e61a661dedeb02fc79e3ffef3ba5b8cc56aed394bba49f4b8`; its production source manifest is `ba3294e08aff1e3542c37e7a6a8a69b84f72a78d804c44824d63a122a7b1d43e`.

A pinned, read-only SQLite online backup produced a coherent **14,995,734,528-byte** V20 database copy in **44.279 seconds**, while the recorder continued running. An earlier unpinned attempt reached its 120-second cooperative deadline and produced no completed copy. No recorder signal was sent. The copy excludes raw journals and encoded media, so it is **not** a complete rollback snapshot. The installed signed app and preferences are retained separately under `~/Library/Application Support/Retrace Candidates/2026-09-12-progressive-recall/`.

The actual V21 migration ran on a separate APFS clone in **4.095 ms**. Full `quick_check` before and after returned `ok`; the checks included existing full-text integrity work and took minutes separately from migration. All six retained core-table counts and existing table definitions were unchanged: **758,094 frames**, **153,913 segments**, **10,331 videos**, **99,394,957 OCR nodes**, and **754,352 document links/indexed documents**. The second migration run changed neither `total_changes()` nor `schema_version`; it created no eager screen observations. The pristine copy and live database were protected by distinct-file checks and were not migrated.

Opening that migrated copy through this Mac's system SQLite **3.51.0** reader failed with `cannot create triggers on shadow tables`. The bundled SQLCipher reader passed. The persisted FTS-shadow triggers conflict with defensive readers; the successful integrity checks do not establish reader compatibility. The correction keeps those triggers in supported writable connections' temporary schemas, retaining normal-table revision triggers in the database. No defensive setting is disabled.

### Corrected migration and final package verification

`RecallSearchRevisionHooks` installs temporary FTS-content revision triggers when `MigrationRunner` opens or reopens the canonical writer, and when `FTSManager` opens its writer. They increment the durable revision inside the text transaction, including rollback behavior. The migration no longer persists triggers on FTS shadow tables. Source/filter identity triggers on ordinary tables remain durable. Missing revision state fails closed once V21 is present.

Five new disk-backed SQLite tests first reproduced **five failures**, including the defensive-reader schema rejection. The corrected focused run passed **37 tests, zero failures**, covering defensive reads, independent writer connections, reopen/no-op migration, transaction rollback, revision fences and existing FTS APIs. A test-only C shim calls the variadic defensive-mode API with its correct ABI; it is excluded from the production Database target. Independent review found no remaining blocker. Focused coverage is **97.67% lines / 100% functions / 94.12% regions** for the new hooks and **100%** for V21.

The corrected actual V21 migration ran on a fresh APFS clone of the pristine V20 copy in **4.695 ms**. Scoped `quick_check` passed before and after for the six retained tables, and after for all 15 new V21 tables. The retained counts and table definitions remained unchanged; the second migration was a no-op and no observations were eagerly materialized. Full FTS integrity was **not repeated** on this second clone; the full checks above remain evidence for the common pristine data. The receipt is `Migration trial database/migration-receipt.json` in the candidate directory.

The corrected copy then passed external-reader acceptance in **1.093 seconds**. A system SQLite **3.51.0** C connection used `READONLY`, `query_only` and verified **DEFENSIVE=1**. It read V21, found zero persisted FTS-shadow triggers and both authored Word text matches, and completed with zero changes. Python readers passed the same bounded checks and five new-table `quick_check` calls. The bundled writer was held open with `query_only=1`, made zero changes and closed cleanly; schema version, file identity/header and file stamp were unchanged. No full-library integrity or legacy-count repeat is implied by this bounded check. All handles closed before the subsequent service probe, and neither live nor pristine database was opened by this reader harness.

A separate system-reader `SQLITE_CANTOPEN` error was reproduced on disposable FTS-only databases **without any V21 objects**, both with zero and 12 reserved bytes per page. On this Mac, system read-only open fails when the last writer has closed and WAL sidecars are absent; it succeeds with an active bundled writer. Reading the quiescent trial copy with `immutable=1` also passed. This is a pre-existing closed-WAL reader limitation, separate from the corrected shadow-trigger schema error. `immutable=1` was used only after all trial writers closed, never on the changing live library. Numeric receipts are `final-reader-acceptance.json` and `closed-wal-reader-diagnosis.json` in the candidate directory.

The final `swift test --enable-code-coverage` passed **826 tests, five intentional skips, zero failures** in **292.073 seconds**. The skips remain explicit real-environment gates. The log is `/tmp/retrace-progressive-recall-final-compatibility-tests.log`. Optimized compilation completed in **439.91 seconds** using `swift build --scratch-path /tmp/retrace-recall-release -c release --product Retrace -Xswiftc -enable-testing --disable-automatic-resolution`.

Corrected candidate **0.7.6 (2609.12.2)** is signed and staged under `~/Library/Application Support/Retrace Candidates/2026-09-12-progressive-recall/`. Its signed executable SHA-256 is `fee2f2553c490cf2881453031cf890fcca2937a9dcacc3d8a955e3ffea67a187`; the unpackaged optimized executable is `759965e7c21042da0532f4a00dcc73434bdf4a7865cc744998b65955ff5c982f`. The current production-input manifest is `fe7a7c9821ff5ae9931e39e6010a7e374086de2cac439e4a8fa8d25ec3004cd4`, covering active local Swift module sources, package manifest/lock, entitlements and UI assets. No listed input changed after the build began. Detailed hashes and test evidence are in `source-and-test-2609.12.2.json`; this is source/build traceability, not a reproducible-build claim.

Independent packaging review passed the app's deep strict signature and separate strict checks of **all 12 Mach-O objects**. Team ID and designated requirement match the installed app; entitlements match both the repository file and installed app. All non-system dependencies resolve inside the bundle, with no build-directory runtime search paths or broken symlinks. All **15 resource files** and modes match the optimized output. The installed **2609.9.1** bundle remains identical to its retained baseline: **112 regular files** and **nine symlink targets**. The receipt is `candidate-2609.12.2-independent-packaging-review.json`.

The staged candidate has not been launched or installed. Its normal runtime shares the live app's preferences/library, and its move/relaunch behavior is not an isolated test harness. The retained database copy excludes media and journals; a controlled installed trial still requires a current compatible recovery set. Source changes remain uncommitted, and Phase 2 has not started.

## Controlled trial preparation — 2026-09-13

The installed **0.7.6 (2609.9.1)** executable and staged **2609.12.2** candidate still matched their recorded hashes and strict signatures. All 245 production and 92 test inputs in the Sept 12 manifest matched before this follow-up began. Preparing the installed trial exposed two further issues: normal AppDelegate Quit only awaited timeline metrics, and a repeated Quit could bypass that wait; the activity notification-latency metric used a timestamp assigned before database COMMIT. Those findings require new regressions and a rebuilt candidate before installation. They do not invalidate the earlier bounded search/media results.

The signed installed app and a private pre-Stop preferences export are retained under `~/Library/Application Support/Retrace Candidates/2026-09-13-progressive-recall-recovery/`. The retained executable hash remains `c9b5756c35e930b97d5a9b5a37b0a312299f49da9f6454f292936d97b4956ea6`. This app/settings copy is **not yet a complete library recovery set**. The current installed recorder remained running during helper development; no candidate launch or live migration is implied.

### Short baseline with authored Word documents

A read-only numeric observer measured the installed **2609.9.1** process from **00:56:03.641 UTC** for 90 seconds, followed by 60 seconds of settling. Two real Word documents shared the title `Cedar proposal`, with independently reviewed authored contents **42000 / DRAFT / NOT GRANTED** and **47000 / SENT / NOT GRANTED**. Both used 170% zoom. CUA verified the document URL and contents after each window switch. The actual switch offsets were **29.708 seconds** and **68.290 seconds**; tool coordination did not achieve the initially proposed 20/20/50-second schedule. A candidate comparison must replay the observed schedule and retain its own actual timings.

The fixed capture-time cohort contained **10 frames**. At settle end, all ten were completed, with zero fresh pending, processing, failed or not-readable rows. Capture-to-processing completion was **p50 1,165 ms / p95 1,886 ms**. The first retained capture was 21.832 seconds into the window; retained-frame gaps reached 25.996 seconds. These are admission/deduplication and processing observations, not proof of uninterrupted activity coverage, OCR correctness, or exact navigation. The separate global failed count rose from 31,799 to 31,853 during observation; 237 frames remained not-readable. Those 54 additional failures are outside the fresh cohort and must not be hidden by its successful result.

Independent review caught an initial CPU-unit error in the temporary observer. On this Mac, the raw process counters require the Mach timebase **125/3** conversion. The corrected cumulative CPU time **146,337.5248 seconds** fell between independent `ps` readings of **146,337.50–146,337.53 seconds** for the same process identity. Original raw CPU percentages are invalid; the original receipt remains unchanged and a separate correction retains its hash. With **100% equal to one core**, the 17 whole workload intervals had **p50 149.07% / p95 223.01%**; 12 settle intervals had **p50 29.30% / p95 189.62%**. One interval crossed the workload boundary and is excluded from those two summaries. These short-window CPU samples do not establish energy, sustained resource use, foreground-action p95 or the planned ten-percent regression gate.

Across 31 samples in the full 150 seconds, RSS reached **1,693.328 MiB** and physical footprint reached **1,248.928 MiB**. Process-written bytes totalled 471,543,808; this is I/O traffic, not retained-media growth. Database logical size grew 339,968 bytes, while allocated DB/WAL/SHM sizes were unchanged. Allowlisted settings and privacy hashes matched at both ends. The Mac was on AC at 100% battery with AC Low Power reported as enabled; no thermal warning was reported, and no temperature was measured. One-minute host load fell from **31.38 to 15.25 on ten CPUs**, a material limitation for any later performance comparison. User workloads were not stopped to manufacture a quiet baseline.

Timely Memory **26.8.176** was running with Tracking On, schedule disabled, 15-minute idle detection, its one-hour fullscreen exception, no keep-capturing app list, and incognito excluded. No settings were changed. Its post-cohort five-item recent list showed two Word `Cedar proposal` labels and a just-now upload. That small status list does not establish distinct document identities, all transitions, durations or a complete Timely timeline comparison. This is a short same-title switching/quiet-reading cohort, not the full Phase 0 Mac/Timely script or an actual-user question corpus.

Private numeric receipts are under `/tmp/retrace-phase1-measurements-20260913/output/`: `baseline-cohort.json`, the separate `baseline-cohort-cpu-corrected.json`, and `baseline-ui-actions.json`. The observer uses read-only/query-only SQLite and never queries OCR or transcript text. Database observation-to-persistence-timestamp deltas remain explicitly separate from a measured durable acknowledgement.

### Shutdown, metrics and test synchronization follow-up

The application now owns its initialization and termination tasks. Confirmed Quit closes recording admission before waiting for metrics, cancels the owned startup task, joins initialization and awaits pipeline shutdown. Repeated Quit requests join that operation. A partial shutdown failure keeps the app open with an error and permits another shutdown attempt; it does not reopen recording admission. Ordinary recording pause/resume remains available before termination begins. Ten real AsyncStream/filesystem lifecycle tests passed after reproducing the cancellation and late-start failures.

Timeline session metrics now retain duration and scrub increments until each database write acknowledges success. Concurrent hide/Quit paths join the same write, cooperative timeouts still drain it, and retries cannot duplicate an acknowledged increment. Shortcut reloads preserve the same metrics owner; a different coordinator is rejected before controller state changes. The combined termination/metrics check passed **20 tests, zero failures** after the configuration regressions reproduced six failures. Focused metric-helper line coverage is **99.21%**. Independent source review has no remaining findings in these changes.

The first full follow-up checkpoint passed **836 tests, five skips, zero failures**. After the additional lifecycle and metrics tests, the next full run executed **851 tests, five skips, one failure**: the existing maintenance test observed three metric inserts before its two-second deadline, rather than four. Logs showed maintenance continuing through the next page. The test now observes two completed explicit SQLite transactions, repeated empty-page cursor resets and bounded node queries, using its existing zero-interval test seam. It retains the two-second deadline and verifies no SQL after the stop/join. Production maintenance code is unchanged. The two focused tests passed in **0.164 seconds**, with independent review complete; a new full-package run is required for the final source state.

The capture latency regression uses a real SQLite store with acknowledgement held after COMMIT, verifies that no success latency is emitted on failure, and covers early test-gate release. The durable latency now ends immediately after `appendActivity` returns using monotonic time. The stored persistence timestamp remains separate. All three focused latency tests passed; native Accessibility coverage remains an independent gate.

### Display setup and normal baseline shutdown

Direct display metadata identified Smart M70D (ID 4, main, 3840×2160 at 1×) and Studio Display (ID 3, 5120×2880 pixels at 2×). Both are external. Word window width and the baseline video's resolution alone do not prove its historical display identity. The unset `captureActiveDisplayOnly` preference is not consumed by the current capture configuration; capture follows the focused window's display with main-display fallback.

Before the next navigation trial, both authored Word windows were placed on Smart M70D and filled that display using Word's normal window controls. Separate screenshots visibly confirmed **42000 / DRAFT** and **47000 / SENT**. A bounded read found fresh frame **50759252**, video **1010351**, encoded sample **63**, with the second document's amount and status indexed. This is a selection aid, not an exact decoder/UI reopen result. The changed window geometry means the earlier short baseline is not a fully matched performance comparison for this setup.

Normal dashboard **Turn Off** was verified as **Off**, and video 1010351 finalized with 64 frames. Open-handle inspection then found no media/audio writers; remaining database handles belonged to the installed app. Normal Quit was confirmed through its dialog, and PID 40169 was verified absent before the quiescent whole-library copy began. No process suspension or forced termination was used. The separately retained immediate pre-Stop preferences export has SHA-256 `5367dd7c842b81a0d70f677fda86655f3c49be9a744ac2f1d202226e1abde497`.

The final source-state test run passed **854 tests, five intentional skips, zero failures** in **288.002 seconds**, with code coverage enabled. The complete log is `/tmp/retrace-progressive-recall-2609.13.1-release-check-tests.log`; the earlier 851-test failure log is preserved separately. The sleep guardrail and `git diff --check` passed. Existing compiler warnings remain. The pre-build manifest pins **247 production inputs, 94 test inputs and five versioned fixtures**; its production manifest SHA-256 is `1e41b263c87282f957a911129d2283ebdcb098c9a27d8debaaab49aabe7df160`. Optimized compilation started at **02:00:46 UTC**; installation still requires its completed package verification and a valid recovery set.

The first whole-library attempt force-cloned **630,183 entries**, then exited **1** with `manifest_mismatch` during final comparison. That attempt remains explicitly incomplete and is not an accepted recovery set. The original library and failed-copy manifests are preserved for diagnosis; no candidate installation or live V21 migration followed the failure.

Optimized compilation subsequently passed in **279.88 seconds**. The final **0.7.6 (2609.13.1)** candidate is signed and staged in the private recovery parent, with executable SHA-256 `7a629c9105acc06ee4878abf94fb5e7a55785846cfed7c1e41255abc9264e9ab`. Independent package review verified all 12 Mach-O signatures, arm64 architecture, the retained signing team and entitlements, bundled dependencies and resources. All 247 production inputs, 94 test inputs and five versioned fixtures still matched the build manifest; the 229 active production Swift sources matched the optimized build description. The full test log, coverage, build log and package log are retained in that parent's `Test and build evidence` directory. This checkpoint establishes a reviewed package, not installation or acceptance.

### Recovery-copy failures and explicit cache policy

The first attempt's **630,184 source-before/source-after manifest rows were identical**. Its sole destination mismatch was the main `retrace.db` group: uid 501/gid 0/mode 0644 became uid 501/gid 20/mode 0644. A disposable ownership probe confirmed that this user cannot assign group 0. The reviewed recovery policy permits only this exact root database group remap, with matching bytes and all other compared metadata. Its receipt must disclose non-exact ownership; no original ownership or privileges were changed. The entire failed attempt was moved using exclusive same-device renames into `Rejected snapshot attempt 1`, preserving its identities and failure status.

The second attempt exited **1** after **41.347 seconds**, with `source_changed` during inventory and **zero entries cloned**. The changed object was `retrace.db-shm`; no destination tree existed. SQLite's WAL index is a regenerable coordination cache, and a real disposable `mode=ro`, query-only connection with zero SQL changes altered that cache while database and WAL hashes remained unchanged. The reader responsible for the live change was not established. The installed app was later observed running again, but its start time was after the attempt had failed and does not explain that earlier change. It was quit normally again at **02:29:31 UTC**, with recording still Off. The second failed attempt remains separately archived with its log and receipts.

A new recovery copy therefore excludes only the root `retrace.db-shm` from its durable-copy contract. It preserves and separately hashes the database, retained WAL and rollback journal, and retains media and capture recovery journals. The original SHM is left untouched. The helper rejects durable-file mutations and writable durable handles, while its receipts disclose the cache exclusion, narrowly permitted root-directory metadata variation and database group remap. An authored fixture proves recovery of a known committed WAL-only marker before migration. Separate validation of the actual copied database must prove initial SHM absence, private WAL-index reconstruction and bounded V20 reads before migration; it has no independently known live WAL-only marker. This follows [SQLite's WAL-index recovery contract](https://www.sqlite.org/walformat.html#the_wal_index_or_shm_file); a read-only SQL connection is not necessarily filesystem-inert. Neither failed attempt is promoted to a completed recovery set.

The first execution of that revised helper stopped during inventory after **39.531 seconds**, again with **zero clones**, because its unknown-sidecar guard also rejected the pre-existing `retrace.db-shm.corrupt-20260723` archive. The original retained manifest confirms that regular 32,768-byte file already existed. This historical archive must remain an ordinary durable file with strict cloning and metadata checks; it is not the excluded active SHM. Attempt 3 was separately archived with its captured exit **1**, unchanged receipt/manifest identities and exact executed helper source. No failed attempt constitutes a recovery set.

### External restart and restored baseline — 2026-09-13

The final native helper passed **56 real disposable tests** and independent review. It narrowly recognizes the exact regular historical archive, retains ordinary metadata/source-change checks for it, and still rejects neighboring unknown names and symlinks. The separate SQLite harness passed **17 preparation tests** plus a fresh committed-WAL fixture: cache reconstruction, V20→V21 migration and second-run no-op, defensive system/Python readers, preserved database/WAL/archive hashes, and three denied writes outside its disposable directory. The fixture's migration took **2.995 ms**, with readers completing in **0.804 seconds**. These are fixture results, not the pending September 13 actual-library validation.

Attempt 4 force-cloned **630,182 entries**, then exited **1** after **347.198 seconds** during database/sidecar verification with `retrace_process_present`. The canonical installed executable started as PID **5427** at **02:57:58 UTC / 12:57:58 Brisbane**. Bounded macOS unified logs identify the immediate launch request as `/usr/bin/open`, PID **5341**, through CoreServicesUIAgent. That short-lived command's caller was not established. The app's watchdog auto-relaunch and launch-at-login settings were not enabled. No automatic retry or candidate installation followed this external-state change.

The whole failed destination, its attempt/before/destination/failure receipts, executed helper source and launch trace are retained in `Rejected snapshot attempt 4`, with same-inode exclusive archive moves verified. All four attempts remain explicitly incomplete; **there is no accepted September 13 whole-library recovery set**. Separate actual-library migration validation, the reviewed guarded bundle swap, candidate launch and installed exact-navigation checks remain unperformed.

Normal recording on installed **0.7.6 (2609.9.1)** was restored through **Resume Capturing**. The Dashboard offered **Turn Off**, and the persisted auto-start preference was true. At **03:04:44 UTC**, bounded read-only inspection found fresh frames **50759253–50759256** with successful encoding and completed processing. The installed executable still matched SHA-256 `c9b5756c35e930b97d5a9b5a37b0a312299f49da9f6454f292936d97b4956ea6`. Activity collection remains at its original disabled/unset preference. Both authored Word windows and the temporary Finder window were closed; Timely Memory was quit normally back to its initial closed state, with its settings preserved. A private `restored-baseline-state-20260913.json` records the final process/preferences check.

The next installed-trial step is to establish an uninterrupted normal Stop/Quit window, run the reviewed native helper into a fresh absent destination, require its captured exit **0** and matching completion receipt, then run the separate SQLite harness and require its own captured exit **0**. Only those results admit the staged **2609.13.1** bundle swap and launch. The external launch caller and actual-user question corpus remain requested information; the code, tests, signed package and reviewed tooling are preserved for continuation.

## Recovery and installed trial — 2026-09-14

At recovery preflight, the installed **2609.9.1** and staged **2609.13.1** executables still matched their retained hashes. All **247 production inputs, 94 test inputs and five fixtures** matched the final tested/build manifest; production source and recovery helpers remained unchanged through this installation. A fresh private preferences export immediately before Stop has SHA-256 `7e4758211855c691365339e5d600194dd3fc18c4934b6e5be07dfb0174fe6a53`. The notification correction described below follows the installed trial.

Normal Dashboard **Turn Off** finalized the latest videos and removed media writers. The original process then quit normally. A subsequent app-targeted screenshot unintentionally relaunched the old app: the screenshot helper activates its named target. The next recovery preflight (attempt 5) rejected that running process before any copy began. This restart has an identified tool cause; it does not establish the caller of the separate September 13 restart. The relaunched process also quit normally, with no forced termination or suspension. Subsequent screenshots use window IDs without activation.

Attempt 6 completed with captured command exit **0** in **542.257 seconds**. It force-cloned **637,280 regular files**, with **222 directories including the root**, representing **82,078,502,418 logical file bytes**. Source-before/source-after durable manifests matched; the database and retained WAL matched source and destination SHA-256 checks. The completion receipt is `native-snapshot-complete.json`, SHA-256 `e766abbc11a95ee16081e09f5627962e74940866fb6eebb69a007c245edfefd6`. No top-level incomplete/rejected marker remained. Earlier failures remain separately retained and are not promoted.

This accepted recovery set follows the reviewed narrow policy above: it excludes only the regenerable root SHM, preserves the historical SHM archive as a durable file, and discloses the root database group remap. It is a same-disk APFS rollback copy with strict durable metadata and database/sidecar hashing, not a physical-disk backup or full media-byte checksum. The source library was not opened by this helper with SQLite. Separate copied-database migration/read validation and the guarded install follow this receipt.

The separate copied-database wrapper then exited **0**, with both migration and reader stages exiting **0**. V20→V21 migration took **13.676 ms**; a second migration was a no-op. The copied database began without SHM and rebuilt its own WAL index. Bounded legacy row identities/content, four selected frames, their associated video/segment/search rows and two authored FTS matches were preserved; scoped checks of the new tables passed. Defensive system and Python readers passed with the disposable writer both active and closed. The completion receipt SHA-256 is `57c19b1e42a1d2bba684b3aacc23e7762e9ef7e32ff9117d343498a15227e517`, bound to the accepted snapshot receipt. No full legacy count/integrity scan was repeated, and neither the live nor preserved recovery database was opened by this harness with SQLite.

The guarded installer exited **0** and completed its exclusive bundle swap at **23:33:48.656 UTC on September 13 / 09:33:48.656 Brisbane on September 14**. It retained the original installed bundle and verified the installed candidate's distinct identity, signature and executable SHA-256 `7a629c9105acc06ee4878abf94fb5e7a55785846cfed7c1e41255abc9264e9ab`. A deliberate normal launch started PID **51773** at **09:33:55 Brisbane**. Read-only live inspection confirmed schema **V21**, one native evidence store and zero activity events while collection was still disabled. Recording and activity collection were then enabled through the installed UI for the authored Word trial. These observations establish installation and launch; the trial checks below determine the accepted behavior.

### Installed same-title evidence and immutable links

The installed Activity & Evidence window exposed the authored second **Cedar proposal** through its **09:37:24–09:37:54** activity interval, with eight verified screen associations. Opening the **09:37:26** screen showed **47000 / SENT / NOT GRANTED**. The selected revision **0** explicitly had no extracted text; opening the separately offered revision **1** showed matching OCR. Actual UI-copied links were retained for both revisions. After closing the window, reopening the revision-0 link retained revision 0 without silently selecting newer text. Reopening the revision-1 link displayed the same second proposal and its matching text.

The first proposal's initial minimal activity observation correctly showed **no verified screen association** and uncertain document identity. A bounded, read-only native Word FTS query for **42000 DRAFT**, restricted to launch through 09:37:22, found frame **50761768**. Its unmodified stored extraction reference opened the **09:36:46** screen in the installed viewer: **42000 / DRAFT / NOT GRANTED**, retained revision **1**, with matching OCR. The viewer's copied link matched that stored reference. Closing the window and reopening this UI-copied link again displayed the first proposal. This establishes a bounded FTS-to-installed-viewer path; the full-screen global search UI was not exercised in this continuation.

The first and second references identify distinct frames (**50761768** and **50761780**) and observations in native store `ED7B5A1F-6967-4801-9F2D-E8EACDD37412`. Visual inspection confirmed the intended recorded pixels and revision labels. This installed test does not claim image-byte equality or all-corpus zero wrong-frame acceptance; the earlier copied-service byte comparisons remain separate evidence. UI copies, reference provenance, reviewed screenshots and hashed manifests are retained privately under `Installed trial 2026-09-14` in the recovery directory. No client document was used.

Stuart continued using the desktop during the trial. At his request, both authored Word windows were reduced to **1100×830** on the left; the evidence window stayed at **1850×1000**, within the left half of the 3840-pixel main display. The evidence window was closed after the link checks. No further fullscreen timeline/search was opened. A controlled Word workload preflight failed its foreground check before measurements started; this was retained as a failed preflight, not a completed matched run.

### Shared-desktop measurement and notification regression

A separate bounded observer completed with exit **0** over a **90-second** workload (**09:41:52.864–09:43:22.864 Brisbane**) and **60-second** settling period. All **7** fresh captures completed, with capture-to-processing p50 **1.087 seconds** and p95/max **2.638 seconds**; no fresh frame remained pending, processing, failed or unreadable. The first retained capture arrived after **22.235 seconds**, and the maximum retained-frame gap was **27.984 seconds**. Outside this fresh cohort, the global historical failure count increased **40155→40168**; the unreadable count stayed **237**. These results do not establish capture coverage or repair of the historical library.

The observer found **1490 activity events**, including **1486 Ghostty events**, in those 90 seconds. Adjacent same-title Ghostty pairs numbered **1451**, with only **11** distinct title hashes but **757** window generations across two window IDs and one process generation. No raw terminal title, URL or document content is included here. The AX callback was resetting window lifetime identity for every title/move notification, causing repeated focus/enrichment pairs. Activity collection was switched **Off** through the UI after this finding; ordinary screen recording remained **On**. The scoped follow-up separates durable window identity from an ephemeral notification revision and adds real notification-route regressions before the next build.

Activity observed-to-pre-COMMIT timestamps measured p50 **2.386 ms**, p95 **10.279 ms** and maximum **53.234 ms**; these are **not** the durable-acknowledgement latency metric. Corrected one-core CPU units measured workload p50 **298.83%**, p95 **380.17%**, and settling p50 **172.41%**, p95 **382.99%**. Maximum RSS was **1,632,927,744 bytes**, maximum physical footprint **1,170,003,392 bytes**, and written I/O **491,823,104 bytes**. Written I/O does not measure retained daily disk growth.

The host was on AC with Low Power Mode enabled; initial load averages were **277.85 / 272.07 / 216.48**. Settings included OCR level **3**, OCR enabled and activity enabled for this measured window. The changed window geometry, concurrent desktop work, high host load and notification defect prevent a matched baseline/candidate or Timely comparison. The observer JSON and failed controlled preflight log are privately retained. The current source is being corrected after the installed 854-test checkpoint; that earlier pass applies to **2609.13.1**, not yet to the follow-up.

### Notification correction — 2026-09-14

The production callback route was extracted unchanged before writing the correction. Its first regression run executed **9 tests**, with **6 failing tests / 11 assertion failures**: twelve unchanged title/move callbacks created thirteen focus visits, thirteen enrichments and thirteen observer refreshes. A stability-only partial fix reduced duplicate visits but still admitted pixels after an A→B→A metadata round trip and complete observation-queue drain. That run executed **17 tests**, with **4 failing tests / 5 assertion failures**. These failures establish why preserving window identity alone is insufficient.

The correction keeps title/move window generations stable while incrementing a separate locked, process-local notification revision. Context sampling and the actual pixel bracket check that revision; enrichment checks it before enqueue and when consumed. Focus/window destruction, process reuse, privacy and lifecycle gaps retain conservative generation invalidation. Title/move callbacks no longer request repeated AX observer replacement. Enrichment is bounded to one in-flight read and one pending resample.

Independent review found a further race in the pending resample: a held A lookup could replay an obsolete B focus after real focus had moved to C. Two additional deterministic SQLite regressions reproduced obsolete B and failed current-C discovery. The final retry holds only a pending flag, reconciles the current app, and carries **document-enrichment-retry** provenance through the sampled event. The test seam joins only an already captured task; it does not mutate production state or substitute timing sleeps for ordering.

The final focused, coverage-enabled command exited **0**, executing **42 tests with zero failures** in **1.940 seconds** after a **72.63-second** build. Comparing complete-suite test identities with 2609.13.1 confirms twelve new regressions across the four existing Capture test files. Native test windows were tiny, offscreen and nonactivating. Independent source and test-log review has no remaining material findings. Aggregate line coverage of the two changed production files is **59.72%**, with the live observer bridge and AX source paths not fully exercised; this is not an overall 80% coverage claim. Notification routing, buffer admission, revision increment and enrichment completion have 100% line/region coverage in that selected run; the pixel sequencer has 100% line coverage and 91.67% region coverage.

Only **Capture/ActivityMonitor.swift** and **Capture/Metadata/CaptureAttribution.swift** changed among the 247 production inputs since 2609.13.1; schema, shared contracts and fixtures remain unchanged. The 2609.14.1 production manifest has SHA-256 `0f3a8c395ec3df401472add76770df71ba48f537f4f909b5f82f888272bcc8d2`, with 229 active repository Swift sources, 94 test inputs and five fixtures. The full suite, optimized build and installed behavior are verified separately below.

The first integrated run exited **1**: **866 tests**, five skips and four assertion failures in one existing maintenance test, over **389.720 seconds**. That test expected two committed maintenance pages within two seconds but observed no completed commit, cursor reset or node-page query. The same unchanged executable passed the isolated test in **1.779 seconds**; completed-page logs appeared 0.777 and 1.391 seconds after coordinator creation. The original failure and isolated pass are both retained. Task admission was not instrumented, so the precise timeout cause and any host-load contribution remain unproven.

The test-only correction gives that existing functional check a bounded **20-second** wait while preserving every SQLite transaction, repeated-page, cursor, bounded-query and joined-shutdown assertion. Production background priority and pacing are unchanged. Independent review approved the amended test (`6c5928a8052959e58190e88baf0efa99718db410dfb26224a5b08470625531ae`). The final source manifest records this additional App test change separately; the production manifest remains unchanged. A complete rerun follows this amendment.

The final complete, coverage-enabled suite exited **0**: **866 tests, five intentional skips, zero failures**, in **441.560 seconds**. The maintenance repetition test passed in **0.375 seconds** within that run. All 247 production inputs, 94 test inputs and five fixtures still matched the final manifest before optimized compilation. The retained full-test log has SHA-256 `d76c0b434b60b1c62687b470b58b7d5852cd89e5cfd0aa72010119e6a4eb68bd`; its coverage JSON has SHA-256 `17a308378d942238c55a585f3330286f64ef706d83193cd93727f1a77e51b047`. The whitespace/diff check and sleep guardrail also pass. These are local source-validation results; installation and runtime measurements remain distinct.

Optimized compilation exited **0** in **300.19 seconds**. All tested inputs remained unchanged, and the optimized build contained the same 229 repository Swift sources. The unpackaged executable SHA-256 is `121f75757bff3eb94ceeccca06e827698920449326ae7aa5fc5346a575a9430f`; signed **0.7.6 (2609.14.1)** has executable SHA-256 `df8f32c5f0914ed16937f0d4b64bf63eff30142fd2b04595e7e606a23b3982e1`. Independent package verification passed deep/strict signatures for the bundle and all twelve Mach-O objects, arm64 support, matching entitlements, internal dependency/symlink resolution and optimized resources. This package adds no model or schema migration.

A normal Quit request exited **0**, and the installed 2609.13.1 process was absent before the installer ran. The reviewed installer rechecked writer quiescence, signatures, hashes and bundle identities, then exited **0** at **10:54:26.904 Brisbane**. It retained **Installed 2609.13.1 before notification fix.app** and performed no library writes. Rollback for this correction restores that app with the current V21 library; restoring the older V20 copy would discard post-snapshot recordings and is not the default rollback. The prior whole-library recovery chain remains retained independently.

An explicit background launch started canonical `/Applications/Retrace.app` as PID **71777** at **10:54:45 Brisbane**. The installed hash matched, schema stayed **V21**, and the native evidence-store UUID was unchanged. The first bounded post-launch check found **16 fresh frames, all completed**, proving recording resumed with its existing autostart preference; activity collection was initially Off. This initial startup check is separate from the timed follow-up cohort.

The installed 2609.14.1 viewer reopened the actual earlier UI-copied links: second proposal revision **0** retained its missing-text state and correct **47000 / SENT** image; second proposal revision **1** showed matching OCR; first proposal revision **1** displayed **42000 / DRAFT** with matching OCR. Both retained **NOT GRANTED**. Frame timestamps and selected revisions remained unchanged across the application replacement. The evidence window stayed at **1850×1000+15+45** on the left and was closed before the background observation. Reviewed screenshots are privately retained; these installed visual checks do not claim image-byte equality or completion of the wider Mac/Timely acceptance gates.

The unchanged numeric observer exited **0** over a **90-second** workload (**10:59:34.783–11:01:04.783 Brisbane**) and **60-second** settling period. It found **5 fresh frames, all completed**, with capture-to-processing p50 **0.601 seconds**, p95/max **1.066 seconds**, and no pending, processing, failed or unreadable fresh frames at settlement. However, the first retained frame was **79.381 seconds** into the workload. There were **48 activity events**: **39 explicit foreground-context-unavailable gaps** and nine WhatsApp focus observations. There were **no Ghostty events**. This cohort therefore neither proves continuous capture coverage nor verifies the earlier Ghostty notification burst under matched native conditions. The missing foreground context was disclosed as gaps; its underlying cause was not established by this aggregate observation.

Observed-to-pre-COMMIT activity timestamps measured p50 **0.819 ms**, p95 **3.491 ms** and maximum **7.012 ms**; they remain distinct from the durable-acknowledgement metric. The global failure count increased **40610→40618** outside the fresh workload cohort; unreadable records remained **237**. Workload one-core CPU p50 was **190.69%**, p95 **366.09%**; settling p50 was **223.13%**, p95 **404.00%**. Maximum RSS was **1,699,528,704 bytes**, physical footprint **1,264,162,688 bytes**, and written I/O **585,113,600 bytes**. This is not retained daily disk growth or a passed resource budget.

The host remained on AC with Low Power Mode and the same recorded OCR/capture settings; initial load averages were **68.61 / 91.46 / 109.69**, substantially different from the prior trial. Both database identity checks passed with no query-budget errors. The changed foreground applications, unavailable-context period and differing host load prevent a before/after performance or Timely claim. The source regressions establish the notification correction; broader native coverage and performance acceptance stay open. After the probe, activity collection was restored **Off** through the UI, ordinary recording remained **On**, and the trial window was closed. The probe JSON, aggregate diagnostics and reviewed image/link evidence remain privately retained.

The final state check at **11:09:16 Brisbane** confirmed canonical PID **71777**, the installed 2609.14.1 executable hash, activity collection Off and recording autostart On. The latest eight sampled post-probe frames (**50763444–50763451**) were all completed. All tested production, test and fixture inputs still matched the final manifest. The exact-link tests and fresh captures are accepted within their stated scope; the actual-user corpus, native unavailable-context investigation, matched Ghostty/Timely exercise and wider correctness/resource gates remain open. At the end of this installed-trial checkpoint, no commit, push or release had been performed.

### Development commit checkpoint — 2026-09-14

Stuart authorised committing the reviewed Phase 0/1 implementation and continuing acceptance work. Before staging, all **247 production inputs, 94 test inputs and five authored fixtures** matched the final 866-test and optimized-build manifest. `git diff --check` and the sleep guard passed. The commit includes the implementation, regression tests, authored fixtures and this evidence record; private trial logs, library copies and installed-app recovery artifacts remain outside the repository.

The next investigation is the installed probe's unavailable foreground context. Phase 2 remains unstarted while Phase 1 acceptance continues. A development commit does not change the installed build or establish completed acceptance or release.
